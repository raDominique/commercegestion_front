import React from 'react';
import usePageTitle from '../../utils/usePageTitle.jsx';

const MesTransactions = () => {
  usePageTitle('Mes transactions');
  return <h1>Mes transactions</h1>;
};

export default MesTransactions;
